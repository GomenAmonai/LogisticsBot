# 🚚 Логистическая платформа

Telegram бот и веб-приложение для управления логистикой с системой ролей (клиент, менеджер, администратор).

## 🏗️ Архитектура проекта

```
IdkProject/
├── bot/                    # Telegram бот
│   ├── handlers/           # Обработчики команд и callback
│   ├── keyboards/         # Inline клавиатуры
│   ├── models/            # Модели данных
│   ├── utils/             # Утилиты
│   ├── database.py        # Работа с БД
│   ├── config.py          # Конфигурация
│   └── main.py            # Точка входа бота
│
├── webapp/                # Веб-приложение
│   ├── app.py             # Flask API сервер
│   ├── react-app/         # React фронтенд
│   │   ├── src/
│   │   │   ├── components/  # React компоненты
│   │   │   ├── services/    # API сервисы
│   │   │   └── App.jsx      # Главный компонент
│   │   └── package.json
│   └── templates/        # HTML шаблоны
│
├── data/                  # База данных (создается автоматически)
├── docker-compose.yml     # Docker конфигурация
├── Dockerfile             # Docker образ
└── requirements.txt       # Python зависимости
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Python зависимости
pip install -r requirements.txt

# React зависимости
cd webapp/react-app
npm install
```

### 2. Настройка

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=ваш_токен_от_BotFather
WEBAPP_URL=http://localhost:5001
```

### 3. Запуск через Docker

```bash
# Запустить все сервисы
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

### 4. Запуск вручную

**Бот:**
```bash
python main.py
```

**Веб-приложение:**
```bash
python run_webapp.py
```

**React (dev режим):**
```bash
cd webapp/react-app
npm run dev
```

## 📋 Функционал

### 👤 Клиент
- Создание заказов через WebApp
- Просмотр своих заказов
- Отслеживание доставок
- Оплата заказов

### 👨‍💼 Менеджер
- Просмотр новых тикетов
- Принятие тикетов
- Управление заказами
- Обновление статусов

### 👑 Администратор
- Управление пользователями
- Просмотр всех заказов
- Статистика системы

## 🗄️ База данных

SQLite база данных создается автоматически в `data/bot_database.db`.

Таблицы:
- `users` - Пользователи с ролями
- `orders` - Заказы
- `tickets` - Тикеты для менеджеров
- `tracking` - История отслеживания
- `payments` - Платежи

## 🔧 Утилиты

```bash
# Проверка конфигурации
python check_config.py

# Создание администратора
python utils/create_admin.py <user_id>
```

## 📝 API Endpoints

- `POST /auth` - Авторизация через Telegram
- `GET /api/orders` - Получить заказы
- `POST /api/orders` - Создать заказ
- `GET /api/tickets` - Получить тикеты (менеджер)
- `POST /api/tickets/<id>/accept` - Принять тикет
- `GET /api/stats` - Статистика

## 🐳 Docker

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Логи
docker-compose logs -f webapp
docker-compose logs -f bot
```

## 📚 Документация

- `README.md` - Этот файл
- `webapp/react-app/README.md` - Документация React приложения

## 🔐 Безопасность

- `.env` файл в `.gitignore`
- Токены не коммитятся в репозиторий
- В продакшене установите `FLASK_SECRET_KEY`

## 📦 Зависимости

**Python:**
- python-telegram-bot
- flask
- flask-cors

**Node.js:**
- react
- vite
- axios
