# 🌐 Руководство по хостингу веб-приложения

## 📋 Варианты хостинга

### 1. Railway (рекомендуется - простой и быстрый)

**Преимущества:**
- ✅ Бесплатный тариф для начала
- ✅ Автоматический HTTPS
- ✅ Простое подключение GitHub
- ✅ Автоматический деплой

**Шаги:**

1. Зарегистрируйтесь на [Railway.app](https://railway.app)

2. Создайте новый проект:
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Подключите ваш репозиторий

3. Настройте переменные окружения:
   ```
   BOT_TOKEN=ваш_токен
   FLASK_SECRET_KEY=случайная_строка
   WEBAPP_PORT=5000
   ```

4. Railway автоматически:
   - Определит Python проект
   - Установит зависимости
   - Запустит приложение
   - Выдаст HTTPS URL

5. Скопируйте URL (например: `https://your-app.railway.app`)

6. Обновите `.env`:
   ```
   WEBAPP_URL=https://your-app.railway.app
   ```

7. В BotFather укажите этот URL

### 2. Render

**Шаги:**

1. Зарегистрируйтесь на [Render.com](https://render.com)

2. Создайте новый Web Service:
   - Connect your repository
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python run_webapp.py`

3. Добавьте переменные окружения

4. Получите HTTPS URL

### 3. Heroku

**Шаги:**

1. Установите Heroku CLI
2. Создайте `Procfile`:
   ```
   web: python run_webapp.py
   ```
3. Деплой:
   ```bash
   heroku create your-app-name
   git push heroku main
   ```

### 4. DigitalOcean / VPS

**Шаги:**

1. Создайте VPS (Droplet)
2. Установите Python, Node.js
3. Настройте Nginx как reverse proxy
4. Установите SSL сертификат (Let's Encrypt)
5. Настройте systemd для автозапуска

## 🔧 Настройка домена

### Подключение домена к Railway/Render:

1. В настройках проекта добавьте ваш домен
2. Настройте DNS записи:
   ```
   CNAME: your-domain.com -> your-app.railway.app
   ```
3. Подождите распространения DNS (до 24 часов)

### Настройка SSL:

- Railway/Render: автоматически
- VPS: используйте Let's Encrypt:
  ```bash
  certbot --nginx -d your-domain.com
  ```

## 📝 Файлы для хостинга

### Procfile (для Heroku):
```
web: python run_webapp.py
```

### runtime.txt (опционально):
```
python-3.9.6
```

### .dockerignore (уже есть):
Исключает ненужные файлы из Docker образа

## 🚀 После деплоя

1. Обновите `.env` с новым URL
2. В BotFather укажите HTTPS URL
3. Перезапустите бота
4. Проверьте работу WebApp

## ⚡ Оптимизация

- Используйте CDN для статических файлов
- Включите кеширование
- Настройте мониторинг (Sentry, etc.)

