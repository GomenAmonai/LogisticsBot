# 🚀 Деплой на хостинг

## ⚡ Быстрый вариант: Railway (рекомендуется)

### Шаг 1: Подготовка

1. Убедитесь, что проект в Git:
```bash
git init
git add .
git commit -m "Initial commit"
```

2. Создайте репозиторий на GitHub и запушьте:
```bash
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

### Шаг 2: Railway

1. Зайдите на https://railway.app
2. Войдите через GitHub
3. New Project → Deploy from GitHub repo
4. Выберите ваш репозиторий

### Шаг 3: Переменные окружения

В Railway добавьте:
```
BOT_TOKEN=ваш_токен
FLASK_SECRET_KEY=случайная_строка_минимум_32_символа
WEBAPP_PORT=5000
```

### Шаг 4: Получите URL

Railway автоматически выдаст HTTPS URL:
```
https://your-app-name.railway.app
```

### Шаг 5: Настройте бота

1. В `.env` (локально):
```
WEBAPP_URL=https://your-app-name.railway.app
```

2. В BotFather:
   - Откройте настройки бота
   - Main App → Enter URL
   - Вставьте: `https://your-app-name.railway.app`
   - Launch Mode: Fullsize
   - Save

### Шаг 6: Подключите домен (опционально)

1. В Railway: Settings → Domains
2. Добавьте ваш домен
3. Настройте DNS:
   ```
   CNAME: your-domain.com -> your-app.railway.app
   ```
4. Подождите распространения DNS (до 24 часов)

## 🔧 Альтернативные варианты

### Render.com

1. Создайте Web Service
2. Build Command: `pip install -r requirements.txt`
3. Start Command: `python run_webapp.py`
4. Добавьте переменные окружения

### VPS (DigitalOcean, AWS, etc.)

1. Установите Python, Node.js, Nginx
2. Настройте Nginx как reverse proxy
3. Установите SSL (Let's Encrypt)
4. Настройте systemd для автозапуска

## 📝 Важно

- ✅ Используйте HTTPS (обязательно для Telegram)
- ✅ Установите FLASK_SECRET_KEY для безопасности
- ✅ Не коммитьте .env файл
- ✅ Настройте мониторинг

## 🐛 Решение проблем

### Приложение не запускается:
- Проверьте логи в Railway
- Убедитесь, что все переменные окружения установлены
- Проверьте, что порт правильный (5000)

### WebApp не открывается:
- Убедитесь, что URL начинается с https://
- Проверьте, что приложение доступно из интернета
- Проверьте логи веб-приложения

